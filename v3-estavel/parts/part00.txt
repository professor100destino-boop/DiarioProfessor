<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <meta name="theme-color" content="#1f6feb" />
  <title>Professor Control</title>
  <style>
:root{
  --bg:#eef3fb;
  --panel:#ffffff;
  --panel-soft:#f8fbff;
  --text:#152235;
  --muted:#6d7a8c;
  --primary:#1f6feb;
  --primary-dark:#123f8a;
  --primary-soft:#e9f2ff;
  --accent:#0f172a;
  --success:#1ea971;
  --warning:#f59e0b;
  --danger:#d94b4b;
  --danger-soft:#ffe7e7;
  --border:#dce5f1;
  --sidebar:#0c1830;
  --sidebar-soft:#13264b;
  --shadow:0 18px 40px rgba(18,31,53,.08);
  --shadow-soft:0 8px 18px rgba(18,31,53,.06);
  --radius:22px;
  --radius-sm:14px;
}

*{box-sizing:border-box}
html,body{margin:0;padding:0}
body{
  font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
  background:radial-gradient(circle at top right,#f8fbff 0,#eef3fb 42%,#eaf0f8 100%);
  color:var(--text);
}
button,input,select,textarea{font:inherit}
button{cursor:pointer}

.app-shell{display:flex;min-height:100vh}

.sidebar{
  width:292px;
  position:fixed;
  inset:0 auto 0 0;
  background:linear-gradient(180deg,var(--sidebar) 0%,#102145 100%);
  color:#fff;
  padding:20px 16px 18px;
  display:flex;
  flex-direction:column;
  z-index:50;
  box-shadow:20px 0 40px rgba(9,20,38,.2);
}
.sidebar-top{display:grid;gap:14px}
.brand{
  display:flex;
  align-items:center;
  gap:12px;
  padding:8px 10px 2px;
}
.brand-mark{
  width:48px;height:48px;border-radius:16px;
  background:linear-gradient(135deg,#2d87ff,#64a8ff);
  display:grid;place-items:center;font-weight:800;letter-spacing:.03em;
  box-shadow:0 10px 25px rgba(31,111,235,.35);
}
.brand strong{display:block;font-size:16px}
.brand small{display:block;color:#9cb0d1;margin-top:4px;font-size:12px}

.profile-card,
.mini-card{
  background:rgba(255,255,255,.07);
  border:1px solid rgba(255,255,255,.08);
  border-radius:18px;
  padding:14px;
}
.profile-card{display:flex;gap:12px;align-items:center}
.profile-avatar{
  width:42px;height:42px;border-radius:14px;
  display:grid;place-items:center;background:rgba(255,255,255,.1);font-size:22px;
}
.profile-card b,.mini-card b{display:block}
.profile-card small,.mini-card small{display:block;color:#aebddb;margin-top:4px;line-height:1.35}

.nav-label{
  margin:22px 10px 10px;
  color:#7f94b4;
  font-size:11px;
  font-weight:800;
  letter-spacing:.14em;
}
.sidebar nav{display:grid;gap:8px}
.nav-item{
  border:0;
  background:transparent;
  color:#d8e1f1;
  padding:12px 14px;
  border-radius:16px;
  display:flex;
  align-items:center;
  gap:12px;
  text-align:left;
  transition:.18s ease;
}
.nav-item:hover{background:rgba(255,255,255,.07);transform:translateX(2px)}
.nav-item.active{
  background:linear-gradient(90deg,rgba(31,111,235,.25),rgba(255,255,255,.08));
  color:#fff;
  box-shadow:inset 0 0 0 1px rgba(255,255,255,.08);
}
.nav-ico{
  width:34px;height:34px;border-radius:12px;
  display:grid;place-items:center;
  background:rgba(255,255,255,.08);
  font-size:16px;
  flex:0 0 34px;
}
.nav-item.active .nav-ico{background:rgba(255,255,255,.16)}
.sidebar-foot{margin-top:auto}

.main{
  flex:1;
  margin-left:292px;
  padding:24px 26px 40px;
}
.topbar{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:16px;
  margin-bottom:20px;
}
.topbar-left{display:flex;align-items:center;gap:14px}
.topbar h1{margin:0;font-size:28px;line-height:1.05}
.topbar p{margin:5px 0 0;color:var(--muted);font-size:14px}
.topbar-actions{display:flex;gap:10px;align-items:center}
.menu-mobile{display:none}

.view{display:none}
.view.active{display:block}
.section-shell{display:grid;gap:16px}
.section-head{
  display:flex;align-items:flex-start;justify-content:space-between;gap:16px;flex-wrap:wrap;
}
.section-head h2{margin:0 0 6px;font-size:24px}
.section-head p{margin:0;color:var(--muted)}

.hero{
  border-radius:28px;
  padding:30px;
  box-shadow:var(--shadow);
  display:grid;
  grid-template-columns:1.4fr .8fr;
  gap:18px;
  overflow:hidden;
  position:relative;
}
.hero::before{
  content:"";
  position:absolute;
  right:-80px;top:-80px;
  width:240px;height:240px;border-radius:50%;
  background:rgba(255,255,255,.08);
}
.hero-dashboard{
  background:linear-gradient(135deg,#08182f 0%,#163d76 50%,#2d77df 100%);
  color:#fff;
}
.hero-copy{position:relative;z-index:1}
.hero-side{display:flex;align-items:stretch;justify-content:flex-end;position:relative;z-index:1}
.eyebrow{display:inline-block;font-size:11px;letter-spacing:.18em;font-weight:800;color:#a7c7ff}
.hero h2{margin:10px 0 10px;font-size:34px;line-height:1.1;max-width:760px}
.hero p{margin:0;max-width:760px;color:#e0ecff;line-height:1.55}
.hero-actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:20px}
.hero-actions{gap:16px}
.hero-action-btn{min-width:220px;min-height:72px;padding:18px 26px !important;border-radius:20px !important;font-size:24px !important;font-weight:800 !important;letter-spacing:-.02em;box-shadow:0 18px 40px rgba(0,0,0,.18)}
.hero-action-btn span{display:block;font-size:13px;font-weight:600;opacity:.95;letter-spacing:0;margin-top:4px}
.hero-action-blue{background:linear-gradient(135deg,#0057b8 0%, #1b75d0 52%, #4ea1ff 100%) !important;color:#fff !important;box-shadow:0 18px 40px rgba(0,87,184,.32) !important}
.hero-action-green{background:linear-gradient(135deg,#0b7d3f 0%, #1d9f58 52%, #4bc67a 100%) !important;color:#fff !important;box-shadow:0 18px 40px rgba(11,125,63,.30) !important}
@media (max-width: 900px){.hero-actions{display:grid;grid-template-columns:1fr 1fr;gap:12px}.hero-action-btn{min-width:0;width:100%;font-size:20px !important;min-height:82px;padding:16px 14px !important}.hero-action-btn span{font-size:12px}}
@media (max-width: 540px){.hero-actions{grid-template-columns:1fr 1fr}.hero-action-btn{font-size:18px !important;min-height:86px;border-radius:18px !important}.hero-action-btn span{font-size:11px}}
.spotlight-card{
  align-self:center;
  background:rgba(255,255,255,.12);
  border:1px solid rgba(255,255,255,.18);
  border-radius:22px;
  padding:18px;
  max-width:280px;
  backdrop-filter:blur(6px);
}
.spotlight-card span{display:block;color:#dce9ff;font-size:12px;margin-bottom:6px}
.spotlight-card strong{display:block;font-size:20px;line-height:1.2;margin-bottom:8px}
.spotlight-card small{display:block;color:#d7e5ff;line-height:1.45}

.cards{
  display:grid;
  grid-template-columns:repeat(4,minmax(0,1fr));
  gap:16px;
  margin:18px 0 16px;
}
.stat{
  background:var(--panel);
  border:1px solid var(--border);
  border-radius:24px;
  padding:20px;
  box-shadow:var(--shadow-soft);
  position:relative;
  overflow:hidden;
}
.stat::after{
  content:"";
  position:absolute;
  right:-24px;bottom:-24px;
  width:92px;height:92px;border-radius:50%;
  background:var(--primary-soft);
}
.stat span{display:block;color:var(--muted);font-size:13px;position:relative;z-index:1}
.stat strong{display:block;font-size:30px;margin-top:8px;position:relative;z-index:1}

.shor