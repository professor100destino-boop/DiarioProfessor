Turma(tid,bim){return db.avaliacoes.filter(a=>a.turmaId===tid&&a.bimestre===bim)}
function calcAluno(alunoId,tid=$('#notasTurma')?.value,bim=$('#notasBimestre')?.value){if(!tid){const a=db.alunos.find(x=>x.id===alunoId);tid=a?.turmaId}const av=avaliacoesTurma(tid,bim||'3º Bimestre'),ativ=av.filter(a=>a.tipo==='atividade'),provas=av.filter(a=>a.tipo==='prova'),n=db.notas[alunoId]||{};const soma=ativ.reduce((s,a)=>s+(Number(n[a.id])||0),0);const atividadeMedia=ativ.length?soma/ativ.length:null;const provasComNota=provas.filter(p=>n[p.id]!==undefined&&n[p.id]!=='');let total=0,div=0;if(atividadeMedia!==null){total+=atividadeMedia;div++}provasComNota.forEach(p=>{total+=Number(n[p.id])||0;div++});return{atividadeMedia,media:div?total/div:null}}

function renderAtividades(){const tid=$('#atividadesTurma')?.value,bim=$('#atividadesBimestre')?.value,alunos=db.alunos.filter(a=>a.turmaId===tid),ativ=avaliacoesTurma(tid,bim).filter(a=>a.tipo==='atividade');if(!alunos.length){$('#atividadesTable').innerHTML='<p class=muted>Nenhum aluno nesta turma.</p>';return}if(!ativ.length){$('#atividadesTable').innerHTML='<p class=muted>Nenhuma atividade cadastrada. Clique em + Nova atividade.</p>';return}const heads=ativ.map(a=>`<th>${esc(a.nome)}<br><small>Valor ${a.valor}</small></th>`).join('');$('#atividadesTable').innerHTML=`<table><thead><tr><th>Aluno</th>${heads}<th>Média das Atividades</th></tr></thead><tbody>${alunos.map(a=>{const n=db.notas[a.id]||{},c=calcAluno(a.id,tid,bim);return `<tr><td><b>${esc(a.nome)}</b></td>${ativ.map(v=>`<td><input type=number min=0 max="${v.valor}" step=.1 data-ativ-aluno="${a.id}" data-ativ-av="${v.id}" value="${n[v.id]??''}"></td>`).join('')}<td><b>${c.atividadeMedia===null?'—':c.atividadeMedia.toFixed(1)}</b></td></tr>`}).join('')}</tbody></table>`;$$('[data-ativ-aluno]').forEach(i=>i.onchange=()=>{db.notas[i.dataset.ativAluno]??={};db.notas[i.dataset.ativAluno][i.dataset.ativAv]=i.value;localStorage.setItem(KEY,JSON.stringify(db));renderAtividades();renderNotas();renderDashboard()})}

function renderNotas(){const tid=$('#notasTurma')?.value,bim=$('#notasBimestre')?.value,alunos=db.alunos.filter(a=>a.turmaId===tid),provas=avaliacoesTurma(tid,bim).filter(a=>a.tipo==='prova');if(!alunos.length){$('#notasTable').innerHTML='<p class=muted>Nenhum aluno nesta turma.</p>';return}const heads=['<th>Média Atividades<br><small>vale 1 nota</small></th>',...provas.map(a=>`<th>${esc(a.nome)}<br><small>Prova • ${a.valor}</small></th>`),'<th>Média Final</th>'].join('');$('#notasTable').innerHTML=`<table><thead><tr><th>Aluno</th>${heads}</tr></thead><tbody>${alunos.map(a=>{const n=db.notas[a.id]||{},c=calcAluno(a.id,tid,bim);return `<tr><td><b>${esc(a.nome)}</b></td><td><b>${c.atividadeMedia===null?'—':c.atividadeMedia.toFixed(1)}</b></td>${provas.map(v=>`<td><input type=number min=0 max="${v.valor}" step=.1 data-prova-aluno="${a.id}" data-prova-av="${v.id}" value="${n[v.id]??''}"></td>`).join('')}<td><span class="badge ${c.media===null?'warn':c.media>=Number(db.config.mediaMin||6)?'ok':'bad'}">${c.media===null?'—':c.media.toFixed(1)}</span></td></tr>`}).join('')}</tbody></table>`;$$('[data-prova-aluno]').forEach(i=>i.onchange=()=>{db.notas[i.dataset.provaAluno]??={};db.notas[i.dataset.provaAluno][i.dataset.provaAv]=i.value;localStorage.setItem(KEY,JSON.stringify(db));renderNotas();renderDashboard()})}

$('#atividadesTurma').onchange=renderAtividades;$('#atividadesBimestre').onchange=renderAtividades;$('#notasTurma').onchange=renderNotas;$('#notasBimestre').onchange=renderNotas;
function addAvaliacao(tipo){const tid=tipo==='atividade'?$('#atividadesTurma').value:$('#notasTurma').value,bim=tipo==='atividade'?$('#atividadesBimestre').value:$('#notasBimestre').value;if(!tid)return toast('Crie uma turma primeiro');modal(tipo==='atividade'?'Nova atividade':'Nova prova',`<label>Nome<input id=mAvNome placeholder="${tipo==='atividade'?'Atividade 1':'Prova 1'}" required></label><label>Valor máximo<input id=mAvValor type=number min=.1 step=.1 value="10" required></label>`,()=>{if(!$('#mAvNome').value)return false;db.avaliacoes.push({id:id(),turmaId:tid,bimestre:bim,tipo,nome:$('#mAvNome').value,valor:Number($('#mAvValor').value||10)});save();return true})}
$('#novaAtividadeBtn').onclick=()=>addAvaliacao('atividade');$('#novaProvaBtn').onclick=()=>addAvaliacao('prova');

function renderConteudos(){const tid=$('#conteudoTurma')?.value;const list=db.conteudos.filter(c=>c.turmaId===tid).sort((a,b)=>b.data.localeCompare(a.data));$('#conteudosList').innerHTML=list.length?list.map(c=>`<div class=list-row><span><b>${new Date(c.data+'T12:00').toLocaleDateString('pt-BR')} — ${esc(c.titulo)}</b><br><small class=muted>${esc(c.conteudo)}</small></span></div>`).join(''):'<span class=muted>Nenhuma aula registrada.</span>'}$('#conteudoTurma').onchange=renderConteudos;$('#novoConteudoBtn').onclick=()=>{const tid=$('#conteudoTurma').value;if(!tid)return toast('Crie uma turma');modal('Registrar aula',`<label>Data<input id=mCData type=date value="${todayISO()}"></label><label>Título<input id=mCTitulo placeholder="Confederação do Equador"></label><label>Conteúdo ministrado<textarea id=mCConteudo></textarea></label><label>Atividade realizada<textarea id=mCAtiv></textarea></label>`,()=>{if(!$('#mCTitulo').value)return false;db.conteudos.push({id:id(),turmaId:tid,data:$('#mCData').value,titulo:$('#mCTitulo').value,conteudo:$('#mCConteudo').value,atividade:$('#mCAtiv').value});save();return true})};
function renderConfig(){$('#profNome').value=db.config.profNome||'';$('#escolaNome').value=db.config.escolaNome||'';$('#mediaMin').value=db.config.mediaMin??6;$('#freqMin').value=db.config.freqMin??75}$('#saveConfigBtn').onclick=()=>{db.config={profNome:$('#profNome').value,escolaNome:$('#escolaNome').value,mediaMin:Number($('#mediaMin').value||6),freqMin:Number($('#freqMin').value||75)};save();toast('Configurações salvas')};$('#resetBtn').onclick=()=>{if(confirm('Isso apagará TODOS os dados deste dispositivo. Continuar?')){db=emptyState();save();toast('Dados apagados')}};
function downloadCSV(name,rows){if(!rows.length)return toast('Sem dados para exportar');const keys=Object.keys(rows[0]);const val=v=>'"'+String(v??'').replaceAll('"','""')+'"';const csv='\ufeff'+[keys.join(';'),...rows.map(r=>keys.map(k=>val(r[k])).join(';'))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'}));a.download=name;a.click();URL.revokeObjectURL(a.href)}
$$('[data-report]').forEach(b=>b.onclick=()=>{const type=b.dataset.report;if(type==='alunos')downloadCSV('alunos.csv',db.alunos.map(a=>{const t=db.turmas.find(x=>x.id===a.turmaId);const mb=db.mediasBimestrais?.[a.id]||{};return{Escola:escolaNome(escolaDaTurma(t)),Turma:turmaNome(t),Numero:a.numero,Nome:a.nome,Matricula:a.matricula,'1º Bimestre':mb['1º Bimestre']??'','2º Bimestre':mb['2º Bimestre']??''}}));if(type==='frequencia')downloadCSV('frequencia.csv',db.frequencias.map(f=>{const a=db.alunos.find(x=>x.